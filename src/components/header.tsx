import styled from 'styled-components';

const Header = () => {
  return <HeaderStyles>Header</HeaderStyles>;
};

/** Example of Styled component in use
 *
 * _Delete if you are not using it_
 */
const HeaderStyles = styled.header`
  margin: 0;
  padding: 0;
`;

export default Header;
